import React from 'react'

function Title(props) {
    return (
        <div className="row">
            <div className="col-lg-12" className="title">{props.title}</div>
        </div>
    )
}

export default Title
